# Set all configuration variables needed to define a reconstruction
# launch in one place.  This file is sourced by the `submit_launch.sh`
# script and can be loaded in Python using the `python-dotenv`
# package.

# set names and run list
RUN_PERIOD="2022-05"  # Run period to process.
BATCH="ver02-perl"  # Batch label of the launch; is appended to workflow name and directory names; should make it clear where/how the campaign was being run.
RUN_NUMBER_MIN=100000  # lower bound of run numbers to process
RUN_NUMBER_MAX=109999  # upper bound of run numbers to process
RCDB_QUERY="@is_cpp_production and @status_approved"  #TODO clarify RCDB query
# RCDB_QUERY="@is_cpp_production and @status_approved==0 and status>0"
LAUNCH_DIR="launch.${RUN_PERIOD}_${BATCH}"  # subdirectory where production scripts are located
RUN_NUMBER_LIST_FILE="list.${RUN_PERIOD}_${BATCH}.txt"  # text file with list of run numbers to process; one run number per line

# set production campaign parameters for JLab site
PRODUCTION_USER="gxproj4"  # production user account name
PRODUCTION_START_DATE="2026-02-19"  # start date of production campaign
PRODUCTION_WORK_DIR="NERSC/${PRODUCTION_START_DATE}.recon.${RUN_PERIOD}_${BATCH}"  # subdirectory in home directory of `${PRODUCTION_USER}` where all production info is stored
PRODUCTION_LAUNCH_DIR="${PRODUCTION_WORK_DIR}/${LAUNCH_DIR}"  # directory where production is launched from at JLab

# set reconstruction parameters
HALLD_VERSION_SET_XML="version_7.5.0.xml"  # XML file that defines the GlueX software version set to be used.
JANA_CONFIG="jana_recon_nersc.${RUN_PERIOD}_${BATCH}.config"  # JANA config file to use; must be located in ${NERSC_LAUNCH_DIR}.
JANA_CALIB_CONTEXT="calibtime=2026-03-05-00-00-01"  # JANA calibration context; overrides any value set in the JANA config file or in the environment.
JANA_GEOMETRY_URL="ccdb:///GEOMETRY/cpp_HDDS.xml"  # JANA geometry to be used in reconstruction; usually `ccdb:///GEOMETRY/main_HDDS.xml` but some data sets such as CPP require custom geometry.

# set swif2 job parameters
SWIF_MAX_CONCURRENT_JOBS=100  # Maximum number of swif2 jobs that can be in-flight at once. This can be set only once when the workflow is created. If jobs are submitted piecemeal by running this script multiple times specifying different run lists, only the first invocation that creates the workflow will set this parameter.
SWIF_RAW_DATA_ROOT="/mss/halld/RunPeriod-${RUN_PERIOD}/rawdata"  # Root of JLab directory tree, where raw data files are located. Must be an `/mss` path.
# SWIF_RAW_DATA_ROOT="/cache/halld/RunPeriod-${RUN_PERIOD}/rawdata"
SWIF_OUTPUT_ROOT="/lustre/expphy/volatile/halld/offsite_prod/RunPeriod-${RUN_PERIOD}/recon/${BATCH}"  # Root of JLab directory tree, where output files are copied to.
SWIF_WORKFLOW="recon_${RUN_PERIOD}_${BATCH}_NERSC-multi"  # Only change this if default name is not appropriate
SWIF_SITE="nersc/perlmutter"  # swif2 site to submit jobs to

# set NERSC job parameters
NERSC_PROJECT="m3120"  # NERSC project to charge to.
NERSC_QOS="regular"  # NERSC queue to use; usually `regular` or `debug`. See NERSC documentation for details on charging and restrictions.
NERSC_NODE_TYPE="cpu"  # Constraint for NERSC jobs; usually `cpu` or `gpu`.
NERSC_MAX_WALL_TIME="5:00:00"  # Maximum wall time for NERSC jobs.
NERSC_MAX_THREADS_PER_TASK=256  # Maximum number of threads available on a NERSC Perlmutter CPU node.
NERSC_NMB_THREADS_PER_PROCESS=32  # Number of threads that each `hd_root` process uses; must be <= ${NERSC_MAX_THREADS_PER_TASK}. Overrides any value set in the JANA config file.
NERSC_NMB_PROCESSES_PER_TASK=$(echo "${NERSC_MAX_THREADS_PER_TASK} / ${NERSC_NMB_THREADS_PER_PROCESS}" | bc)  # Number of hd_root processes to run concurrently on a single NERSC Perlmutter CPU node.  #TODO works only if division is exact; need to round up if not exact
NERSC_HOST="perlmutter-p1.nersc.gov"  # NERSC hostname to use for ssh.
NERSC_CONTAINER_IMAGE="docker:jeffersonlab/gluex_almalinux_9:latest"  # Shifter image that was converted from Docker image. Is not pulled in automatically and needs to exist in Shifter registry.
NERSC_PROJECT_DIR="/global/cfs/cdirs/${NERSC_PROJECT}"  # Project directory in the NERSC Common File System (CFS) where config files and scripts will be copied to and run from.
NERSC_LAUNCH_DIR="${NERSC_PROJECT_DIR}/${LAUNCH_DIR}"  # NERSC directory that will get mapped to `${NERSC_LAUNCH_DIR_CONTAINER}` inside the task container. Contains scripts and JANA config file to run hd_root.
NERSC_LAUNCH_DIR_CONTAINER="/${LAUNCH_DIR}"  # directory inside the container that contains scripts and JANA config file to run hd_root

# variables in the original script that are not used in the new version but might be needed in the future:
# TESTMODE
# VERBOSE
# LAUNCHTYPE
# NAME
# TIMELIMIT
# SCRIPTFILE
